# AI 提效比赛案例

## 案例名称

**一键截图秒出文字：用 Vibe Coding 打造 macOS OCR 提效工具**

---

## 案例描述

通过 AI 辅助编程（Vibe Coding）快速开发了一款 macOS 原生 OCR 工具，解决日常办公中文字无法复制的高频痛点。技术上采用 Swift + SwiftUI 开发，集成智谱 GLM-OCR 模型实现高精度识别，支持全局快捷键截图、剪贴板图片自动监听、历史记录管理。开发采用 Plan-Craft 工作流，AI 完成约 80% 编码，人负责需求和架构决策，从零到可用仅数天。最终实现一键触发、识别结果自动入剪贴板的轻量工具，API 成本仅 3 元/半年，远低于商业软件月费 8 元。

---

## 场景与痛点

日常办公中文字提取是高频需求，但存在多个瓶颈：

1. **文档被保护**：PRD 等文档禁止复制，只能手动录入；
2. **Excel 内容溢出**：单元格显示不全，需逐个点击查看；
3. **网页禁止复制**：部分页面禁用右键和选中；
4. **工具链路长**：macOS 原生 OCR 需保存图片再识别，微信截图需额外步骤提取文字，多工具快捷键冲突；
5. **商业软件贵**：订阅制月费 8 元起，长期累积成本高。

以上导致每次文字提取需经历「截图→保存→上传→识别→复制」多步操作，日均浪费 10-15 分钟。

---

## AI 应用解决方案

方案在两个层面应用 AI：

### 一、AI 辅助开发（Vibe Coding）

采用 Plan-Craft 工作流：ChatGPT 进行需求分析 → Claude Plan 模式输出开发计划 → 人工审查 → Craft 模式执行编码。将需求拆解为截图服务、OCR 服务、快捷键管理、剪贴板监听等独立模块逐个交付，减少返工和 token 消耗。配合主动备份策略防止 AI 误改导致崩溃。

### 二、AI 识别能力集成

- **智谱 GLM-OCR**：调用版面解析 API，支持文字和公式识别，输出 Markdown 结构化结果；
- **全局快捷键**：基于 Carbon EventTap 实现系统级热键（Command+Shift+R），无需切换应用；
- **剪贴板监听**：实时检测剪贴板图片变化，自动触发 OCR，实现"复制图片即识别"；
- **系统栏常驻**：SwiftUI MenuBar 应用，不干扰工作流，结果通过通知反馈并自动写入剪贴板；
- **API Key 加密存储**：保障敏感信息安全。

**核心创新**：快捷键截图 + 剪贴板监听双通道触发，将"截图→识别→复制"三步合一。

---

## 效果与价值

1. **操作效率**：文字提取从 6 步缩减为 1 步（按快捷键框选即完成），单次节省 15-30 秒，日均节省 10-15 分钟；
2. **开发效率**：AI 承担 80% 编码，开发周期从预估 2-3 周缩短至数天；
3. **成本极低**：API 半年约 3 元，相比商业软件节省超 90%；
4. **体验流畅**：全局快捷键触发，结果自动入剪贴板，全程不打断工作流；
5. **方法论沉淀**：形成 Plan→审查→Craft→备份的 Vibe Coding 实践范本，可供团队参考。

---

## 可复用性说明

1. **方法论通用**：Plan-Craft 工作流、主动备份策略适用于任何 AI 辅助开发项目；
2. **架构可扩展**：截图 + OCR + 剪贴板的模式可衍生为翻译工具、公式识别器、名片扫描等应用；
3. **API 可替换**：OCR 服务层协议化设计，可对接百度、腾讯云、Google Vision 等供应商；
4. **跨平台可迁移**：核心设计模式（API 调用、剪贴板监听、快捷键）可移植到 Windows/Linux；
5. **团队可部署**：配合统一 API Key 管理，可在团队内推广使用。
